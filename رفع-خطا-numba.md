# 🔧 رفع خطای `numba` در رندر

## چه شد؟

```
RuntimeError: Cannot install on Python version 3.14.3;
only versions >=3.10,<3.14 are supported.
ERROR: Failed to build 'numba'
==> Build failed 😞
```

**دو مشکل همزمان:**

۱. رندر پایتون **۳.۱۴** گرفت (جدیدترین)، ولی کتابخانهٔ `numba`
   هنوز روی آن ساخته نمی‌شود.

۲. رندر از **`requirements.txt`** استفاده کرد نه
   `requirements-web.txt` — و آن فایل `pandas-ta` دارد که
   `numba` می‌خواهد.

خطا از کد شما نبود. از نسخهٔ پایتون بود.

---

## ✅ رفع شد — سه کار کردم

| کار | فایل |
|---|---|
| قفل پایتون روی ۳.۱۲.۷ | `runtime.txt` + `.python-version` |
| جدا کردن هستهٔ سبک از کتابخانه‌های سنگین | `requirements.txt` |
| بکتست حالا اختیاری است، کرش نمی‌کند | `web_api.py` |

> `render.yaml` می‌گفت پایتون ۳.۱۲ باشد، ولی وقتی سرویس را
> **دستی** می‌سازید رندر آن را نادیده می‌گیرد. فایل `runtime.txt`
> را همیشه می‌خواند.

**در محیطی دقیقاً مثل رندر تست شد:** ۸ از ۸ روت سبز ✅

---

# 📋 کاری که باید بکنید

## گام ۱ — فایل‌های اصلاح‌شده را در گیت‌هاب بگذارید

چهار فایل عوض شده‌اند:

```
runtime.txt          ← جدید
.python-version      ← جدید
requirements.txt     ← اصلاح شد
web_api.py           ← اصلاح شد
```

### روش کشیدن‌ورها (مثل دفعهٔ قبل)

۱. فایل `dow-analyzer.zip` جدید را از این گفتگو دانلود کنید
۲. از حالت زیپ درش بیاورید
۳. در صفحهٔ مخزن گیت‌هاب: **Add file** ← **Upload files**
۴. همهٔ فایل‌ها را بکشید داخل
۵. پایین: **Commit changes**

گیت‌هاب فایل‌های تکراری را خودش به‌روز می‌کند. نگران نباشید.

> ⚠️ فایل `runtime.txt` حتماً باید بالا برود. اگر نرود، خطا تکرار
> می‌شود.

---

## گام ۲ — Build Command را درست کنید

در رندر: **Settings** ← بخش **Build & Deploy**

روی **Build Command** ← **Edit** و مطمئن شوید **دقیقاً** این باشد:

```
pip install -r requirements-web.txt
```

☝️ **`-web` حتماً باشد.** این مهم‌ترین قسمت است.

**Save changes** را بزنید.

---

## گام ۳ — دوباره اجرا کنید

بالای صفحهٔ سرویس: **Manual Deploy** ← **Deploy latest commit**

---

## ⏳ این بار چه می‌بینید

```
==> Using Python version 3.12.7        ← ✅ نشانهٔ خوب
==> Installing dependencies
    Successfully installed flask gunicorn yfinance ...
==> Build successful 🎉
==> Starting service
[+] موتور رصد زنده فعال شد
==> Your service is live 🎉
```

**دو چیز را چک کنید:**

- خط `Using Python version 3.12.7` — اگر `3.14` نوشته، `runtime.txt`
  آپلود نشده
- هیچ‌جا کلمهٔ `numba` نباشد

---

## ❓ «بکتست» چه شد؟

قابلیت بکتست روی سایت **کار نمی‌کند** و به‌جایش پیام می‌دهد:

> بکتست در نسخه وب فعال نیست — این قابلیت را روی کامپیوتر خودتان
> اجرا کنید

**چرا؟** چون کتابخانه‌اش (`backtesting`) به `numba` وابسته است و
همان مشکل را برمی‌گرداند.

**مهم است؟** نه زیاد. بکتست یک‌بار اجرا می‌شود تا استراتژی سنجیده
شود — کار روزمره نیست. بقیهٔ داشبورد کامل کار می‌کند: تحلیل زنده،
سیگنال، برگهٔ معامله، اخبار، احساسات، آموزش.

روی کامپیوتر خودتان همچنان کامل کار می‌کند.

---

## 🆘 اگر باز خطا داد

| پیام | درمان |
|---|---|
| هنوز `Python version 3.14` | `runtime.txt` آپلود نشده — گام ۱ |
| هنوز `numba` | Build Command `-web` ندارد — گام ۲ |
| `No module named 'wsgi'` | `wsgi.py` آپلود نشده |
| `Out of memory` | Build Command هنوز `requirements.txt` است |

**متن کامل خطا را بفرستید** — همیشه ۲۰ خط آخر لاگ مهم است.
