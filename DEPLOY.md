# 🚀 راه‌اندازی ثبت خودکار ابری

هدف: کارنامهٔ سیگنال‌ها بدون هیچ سروری و بدون باز بودن داشبورد پر شود.

**زمان لازم: حدود ۱۰ دقیقه. هزینه: صفر. کارت اعتباری: لازم نیست.**

---

## چرا گیت‌هاب اکشنز و نه سرور ابری؟

قبل از انتخاب، گزینه‌ها را واقعاً بررسی کردیم:

| گزینه | مشکل |
|---|---|
| **Render رایگان** | ❌ فایل‌سیستم موقتی است — هر ری‌استارت `journal.jsonl` را **پاک می‌کند**. بعد از ۱۵ دقیقه بی‌ترافیکی هم می‌خوابد و نخ ثبت با آن می‌میرد. |
| **Fly.io رایگان** | ❌ تیر رایگان در ۲۰۲۴ حذف شد. الان فقط ۲ ساعت آزمایشی، بعدش کارت اعتباری. |
| **Oracle Cloud** | 🟡 واقعاً رایگان و قدرتمند، ولی کارت اعتباری برای احراز هویت می‌خواهد و راه‌اندازی‌اش یک بعدازظهر کار است. |
| **گیت‌هاب اکشنز** | ✅ رایگان، بدون کارت، و ژورنال در ریپو commit می‌شود. |

خطر پنهان رندر مهم است: سرویس بالا می‌ماند و سالم به نظر می‌رسد،
ولی داده‌ها بی‌سروصدا پاک می‌شوند. یعنی **فکر می‌کنید نمونه جمع
می‌کنید در حالی که هیچ ندارید**.

یک مزیت جانبی گیت‌هاب: تاریخچهٔ گیت **سند حسابرسی** می‌شود. هر نمونه
با زمان و هش ثبت می‌شود و بعداً قابل دستکاری نیست.

---

## گام ۱ — ساخت ریپوی خصوصی

۱. به [github.com/new](https://github.com/new) بروید
۲. نام: `dow-analyzer` (یا هر چه دوست دارید)
۳. **Private** را انتخاب کنید ✅
۴. هیچ گزینهٔ دیگری را تیک نزنید (نه README، نه gitignore)
۵. **Create repository**

---

## گام ۲ — آپلود کد

در پوشهٔ پروژه این دستورها را اجرا کنید — `USERNAME` و `REPO` را
عوض کنید:

```bash
cd dow_analyzer

git init
git add .
git commit -m "نسخه اولیه"
git branch -M main
git remote add origin https://github.com/USERNAME/REPO.git
git push -u origin main
```

> ⚠️ **فایل `.env` آپلود نمی‌شود** چون در `.gitignore` است. این عمدی
> است — کلیدها هرگز نباید در ریپو باشند. در گام ۳ به‌صورت امن اضافه
> می‌شوند.

### نمونه‌های موجود را هم ببرید

`journal.jsonl` هم در `.gitignore` است، پس نمونه‌هایی که تا الان جمع
شده‌اند همراه کد نمی‌روند. اگر می‌خواهید از صفر شروع نکنید، یک بار
با `-f` اضافه‌شان کنید:

```bash
git add -f journal.jsonl
git commit -m "نمونه های موجود"
git push
```

اگر ترجیح می‌دهید ابر از صفر و تمیز شروع کند، این گام را رد کنید —
ورک‌فلو خودش فایل را می‌سازد.

اگر گیت‌هاب رمز خواست، رمز حساب کار نمی‌کند؛ باید
[Personal Access Token](https://github.com/settings/tokens) بسازید
(اسکوپ `repo`) و آن را به‌جای رمز بزنید.

---

## گام ۳ — افزودن کلیدها

در صفحهٔ ریپو:

**Settings → Secrets and variables → Actions → New repository secret**

سه کلید را یکی‌یکی اضافه کنید. مقدارشان در فایل `.env` محلی شماست:

| نام (دقیقاً همین) | از کجا |
|---|---|
| `FINNHUB_KEY` | خط `FINNHUB_KEY=` در `.env` |
| `ALPHAVANTAGE_KEY` | خط `ALPHAVANTAGE_KEY=` در `.env` |
| `FRED_KEY` | خط `FRED_KEY=` در `.env` |

برای دیدن مقادیر: `cat .env`

> این کلیدها **اختیاری‌اند**. بدون آن‌ها هم ثبت کار می‌کند، چون
> منابع اصلی (یاهو، CFTC، سوئیس‌کوت) کلید نمی‌خواهند. فقط بعضی
> لایه‌های کلان محدودتر می‌شوند.

---

## گام ۴ — اجازهٔ نوشتن

**Settings → Actions → General → Workflow permissions**

گزینهٔ **Read and write permissions** را انتخاب و **Save** کنید.

بدون این، ورک‌فلو اجرا می‌شود ولی `commit` شکست می‌خورد و
**داده‌ها ذخیره نمی‌شوند**.

---

## گام ۵ — اولین اجرا (دستی)

**تب Actions → «ثبت سیگنال» → Run workflow → Run workflow**

حدود یک تا دو دقیقه طول می‌کشد. روی اجرا کلیک کنید تا لاگ را ببینید:

```
[13:05:11] شروع ثبت دوره ای
[13:05:12] ژورنال پیش از اجرا: 6 نمونه
[13:05:19] ثبت US30   1h  — امتیاز -17.29
[13:05:24] ثبت XAUUSD 1h  — امتیاز +3.41
[13:05:26] ارزیابی: 2 سیگنال تازه سنجیده شد
[13:05:26] ژورنال پس از اجرا: 8 نمونه (+2)
```

اگر بازار بسته باشد همه «رد» می‌شوند — این **درست** است، نه خطا.

پایین صفحهٔ اجرا هم یک جدول خلاصه می‌بینید.

---

## گام ۶ — همین. 🎉

از این به بعد خودکار است:

| تنظیم | مقدار |
|---|---|
| زمان‌بندی | هر ساعت، دوشنبه تا جمعه |
| بازهٔ ساعتی | ۱۳ تا ۲۱ UTC = ۰۹:۰۰ تا ۱۷:۰۰ نیویورک |
| مصرف سهمیه | ~۳۰۰ دقیقه از ۲۰۰۰ دقیقهٔ رایگان (**۱۵٪**) |
| ذخیره | commit خودکار `journal.jsonl` + artifact ۹۰ روزه |

---

## دیدن نتیجه‌ها

### روش ۱ — در گیت‌هاب
تب **Actions**، هر اجرا یک جدول خلاصه دارد.

### روش ۲ — محلی (توصیه‌شده)
هر چند روز ژورنال را بکشید و داشبورد را باز کنید:

```bash
git pull
python server.py --port 8080
```

بعد کارت «کارنامه سیگنال‌ها» را باز کنید.

### روش ۳ — خط فرمان

```bash
git pull
python journal.py summary
```

---

## عیب‌یابی

**قبل از هر push این را اجرا کنید:**

```bash
python deploy_check.py
```

چهار چیز را می‌سنجد: فایل‌های لازم، نشت کلید، صحت ورک‌فلو، و یک
اجرای واقعی. اگر سبز بود، آماده است.

| نشانه | علت | درمان |
|---|---|---|
| مرحلهٔ commit قرمز شد | دسترسی نوشتن نیست | گام ۴ را انجام دهید |
| همه «رد — بازار بسته» | تعطیلی یا آخر هفته | طبیعی است |
| همه «رد — زیر آستانه» | سیگنال ضعیف بود | طبیعی است |
| اجرا اصلاً شروع نشد | زمان‌بندی تا ۱۵ دقیقه تأخیر دارد | صبر کنید |
| بعد از ۶۰ روز متوقف شد | قانون بی‌فعالیتی گیت‌هاب | چون خودمان commit می‌کنیم پیش نمی‌آید |
| `Too Many Requests` در لاگ | یاهو IP گیت‌هاب را محدود کرد | معمولاً موقتی؛ اگر ادامه یافت بازهٔ cron را کم کنید |

---

## نکتهٔ صادقانه دربارهٔ سرعت

ثبت خام با نمونهٔ آماری یکی نیست. سیگنال ساعت ۱۰ با افق ۸ کندل تا
ساعت ۱۸ سنجیده می‌شود؛ سیگنال ساعت ۱۱ تا ساعت ۱۹. این دو **۷ کندل
از ۸ کندل را مشترک دارند** — دو ردیف در فایل، ولی تقریباً یک مشاهده.

نرخ واقعی نمونهٔ مستقل: **حدود ۴ در روز معاملاتی**.

| هدف | زمان واقعی |
|---|---|
| ۳۰ نمونه مستقل | ~۷ روز معاملاتی (~۱۰ روز تقویمی) |
| ۱۰۰ نمونه مستقل | ~۲۴ روز معاملاتی (~۵ هفته) |

۳۰ نمونه حداقلی است که نرخ برد را از شانس محض تفکیک کند — **نه آمار
قطعی**. برای اعتماد به جدول «اثر هر بخش» صدها نمونه لازم است.
