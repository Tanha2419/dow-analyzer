# 🖥️ صفحهٔ تنظیمات رندر — هر گزینه چیست

از بالای صفحه به پایین می‌رویم.

---

## ۱) Source Code

```
  ✅ نام مخزن شما را نشان می‌دهد
```

**دست نزنید.** درست است.

---

## ۲) Name

```
  dow-analyzer
```

اسم آدرس سایتتان می‌شود: `dow-analyzer.onrender.com`

اگر کسی قبلاً این اسم را گرفته باشد، خطا می‌دهد — چیزی مثل
`dow-analyzer-ali` بگذارید.

---

## ۳) Project *(اختیاری)*

**خالی بگذارید.** فقط برای دسته‌بندی است.

---

## ۴) Language

```
  Python 3
```

باید خودکار پر شده باشد. اگر `Docker` یا `Node` نوشته، عوضش کنید.

---

## ۵) Branch

```
  main
```

اگر `master` نوشته هم اشکالی ندارد — همان را بگذارید.

---

## ۶) Region

```
  Oregon (US West)
```

هر کدام باشد فرقی نمی‌کند. دست نزنید.

---

## ۷) Root Directory

```
  (خالی)
```

⚠️ **حتماً خالی بماند.**

اگر چیزی داخلش هست پاکش کنید — مگر اینکه موقع آپلود، فایل‌ها داخل
یک پوشه رفته باشند. در آن صورت اسم آن پوشه را بنویسید.

> 🔍 **چک کنید:** در صفحهٔ اول مخزن گیت‌هاب، آیا مستقیم `agent.py`
> و `server.py` را می‌بینید؟ اگر بله، خالی بگذارید.

---

## ۸) Build Command ⭐ مهم

```
pip install -r requirements-web.txt
```

⚠️ **دقت کنید `-web` داشته باشد.**

اگر نوشته `pip install -r requirements.txt` (بدون `-web`)،
**عوضش کنید**. آن فایل کتابخانه‌های سنگین دارد و حافظه تمام می‌شود.

---

## ۹) Start Command ⭐⭐ مهم‌ترین

```
gunicorn wsgi:app --bind 0.0.0.0:$PORT --workers 1 --threads 4 --timeout 120 --access-logfile -
```

اگر چیز دیگری نوشته (مثلاً `python server.py`)، **همهٔ کادر را پاک
کنید** و خط بالا را عیناً بچسبانید.

### این خط یعنی چه؟

| بخش | کار |
|---|---|
| `gunicorn wsgi:app` | سرور حرفه‌ای را روی برنامهٔ ما اجرا کن |
| `--bind 0.0.0.0:$PORT` | به پورتی که رندر می‌دهد گوش بده |
| `--workers 1` | یک پردازه (بیشتر در ۵۱۲ مگ جا نمی‌شود) |
| `--threads 4` | ۴ درخواست همزمان |
| `--timeout 120` | تا ۲ دقیقه صبر کن (بعضی تحلیل‌ها کندند) |

---

## ۱۰) Instance Type

```
  ● Free     $0/month
```

**حتماً Free.** اگر گزینهٔ دیگری انتخاب شده، عوضش کنید.

---

## ۱۱) Environment Variables

اینجا **فعلاً هیچ‌کاری نکنید**. بعد از اینکه سایت بالا آمد اضافه
می‌کنیم.

> اگر `render.yaml` را خوانده باشد، سه تا خالی می‌بینید
> (`FINNHUB_KEY` و غیره). اشکالی ندارد — خالی بمانند.

---

## ۱۲) Advanced *(اگر باز است)*

| گزینه | مقدار |
|---|---|
| Health Check Path | `/api/health` |
| Auto-Deploy | `Yes` |

---

# ✅ حالا دکمه را بزنید

```
   [ Deploy Web Service ]
```

---

# ⏳ بعدش چه می‌شود؟

صفحهٔ لاگ باز می‌شود و متن سبز و سفید می‌آید. **۵ تا ۱۰ دقیقه**
طول می‌کشد. نبندیدش.

### مراحلی که می‌بینید

```
==> Cloning from https://github.com/...        ← گرفتن کد
==> Installing dependencies                    ← ۳-۷ دقیقه، طولانی‌ترین
==> Build successful 🎉
==> Starting service                           ← بالا آمدن
[+] موتور رصد زنده فعال شد                     ← پیام خودمان!
==> Your service is live 🎉                    ← تمام!
```

وقتی `Your service is live` را دیدید، آدرس بالای صفحه را باز کنید.

---

# 🆘 اگر قرمز شد

| پیام | معنی | درمان |
|---|---|---|
| `No such file or directory: requirements-web.txt` | فایل آپلود نشده | Build Command را به `pip install -r requirements.txt` عوض کنید |
| `ModuleNotFoundError: No module named 'wsgi'` | `wsgi.py` آپلود نشده یا Root Directory اشتباه است | گام ۷ را چک کنید |
| `Out of memory` | کتابخانهٔ سنگین نصب شده | Build Command باید `-web` داشته باشد |
| `Application exited early` | خطای کد | ۲۰ خط آخر لاگ را بفرستید |
| `Port scan timeout` | به `$PORT` گوش نداده | Start Command را دوباره چک کنید |

**هر خطایی دیدید، متن لاگ یا عکس صفحه را بفرستید.**

---

# 📌 دو نکته برای بعد

۱. **کارنامه روی سایت خالی است** — این طبیعی است. ثبت کار گیت‌هاب
   اکشنز است، نه رندر. (دلیلش در `RENDER.md`)

۲. **UptimeRobot را بعداً وصل کنید** — آدرسش باید
   `آدرس-شما/api/ping` باشد، نه آدرس اصلی.
