# 🚀 حساب گیت‌هاب ساختید؟ از اینجا شروع کنید

**کد آماده و کامیت‌شده است.** فقط باید به حساب شما وصل شود.

⏱️ حدود ۵ دقیقه · هیچ دانش گیت لازم نیست

---

## گام ۱ — ساخت ریپو (۱ دقیقه)

۱. به [github.com/new](https://github.com/new) بروید
۲. **Repository name**: `dow-analyzer`
۳. **Private** را انتخاب کنید 🔒
۴. ❗ سه گزینهٔ پایین را **تیک نزنید** (README / .gitignore / license)
۵. **Create repository**

صفحه‌ای می‌آید که می‌گوید «…or push an existing repository».
آدرسی شبیه این در آن هست — کپی‌اش کنید:

```
https://github.com/USERNAME/dow-analyzer.git
```

---

## گام ۲ — ساخت توکن دسترسی (۲ دقیقه)

گیت‌هاب دیگر رمز عبور را برای push قبول نمی‌کند. یک توکن لازم است:

۱. [github.com/settings/tokens/new](https://github.com/settings/tokens/new)
۲. **Note**: `dow-analyzer`
۳. **Expiration**: `90 days` (یا بیشتر)
۴. زیر **Select scopes** فقط این دو را تیک بزنید:
   - ✅ `repo`
   - ✅ `workflow`
۵. پایین صفحه: **Generate token**
۶. توکنی که نشان می‌دهد را **همان لحظه کپی کنید** — دیگر نشانش
   نمی‌دهد. با `ghp_` شروع می‌شود.

> ⚠️ این توکن مثل رمز عبور است. در چت ننویسیدش.

---

## گام ۳ — آپلود (۱ دقیقه)

در ترمینال، داخل پوشهٔ پروژه:

```bash
cd dow_analyzer

git remote add origin https://github.com/USERNAME/dow-analyzer.git
git push -u origin main
```

`USERNAME` را با نام کاربری خودتان عوض کنید.

وقتی پرسید:

| سؤال | جواب |
|---|---|
| `Username` | نام کاربری گیت‌هاب شما |
| `Password` | **توکن `ghp_...`** — نه رمز عبور |

> اگر خطای `remote origin already exists` گرفتید:
> `git remote set-url origin https://github.com/USERNAME/dow-analyzer.git`

---

## گام ۴ — اجازهٔ نوشتن ⚠️ مهم‌ترین گام

در صفحهٔ ریپو:

**Settings** ← **Actions** ← **General** ← پایین صفحه:
**Workflow permissions**

گزینهٔ **Read and write permissions** را انتخاب و **Save** کنید.

> بدون این، ورک‌فلو سبز می‌شود ولی **هیچ داده‌ای ذخیره نمی‌شود**.
> این ساکت‌ترین و بدترین حالت شکست است.

---

## گام ۵ — کلیدها (اختیاری، ۱ دقیقه)

**Settings** ← **Secrets and variables** ← **Actions** ←
**New repository secret**

سه کلید از فایل `.env` محلی‌تان:

| نام | از کجا |
|---|---|
| `FINNHUB_KEY` | `cat .env` |
| `ALPHAVANTAGE_KEY` | همان‌جا |
| `FRED_KEY` | همان‌جا |

> این‌ها **اختیاری‌اند**. منابع اصلی (یاهو، CFTC، سوئیس‌کوت) کلید
> نمی‌خواهند. بدون این‌ها هم ثبت کار می‌کند.

---

## گام ۶ — اولین اجرا 🎉

**تب Actions** ← **«ثبت سیگنال»** ← **Run workflow** ←
دکمهٔ سبز **Run workflow**

یکی دو دقیقه صبر کنید، بعد روی اجرا کلیک کنید تا لاگ را ببینید.

### اگر بازار باز باشد:

```
[13:05:12] ژورنال پیش از اجرا: 0 نمونه
[13:05:19] ثبت US30   1h  — امتیاز -17.29
[13:05:24] ثبت XAUUSD 1h  — امتیاز +3.41
[13:05:26] ژورنال پس از اجرا: 2 نمونه (+2)
```

### اگر بازار بسته باشد (آخر هفته):

```
[13:05:12] رد XAUUSD 1h  — بازار بسته
[13:05:12] رد US30   1h  — بازار تعطیل آخر هفته
```

**این هم موفقیت است** — یعنی گیت درست کار می‌کند و قواعد بازار
رعایت می‌شوند. آخر هفته نباید چیزی ثبت شود.

---

## تمام شد ✅

از این به بعد **هر ساعت، دوشنبه تا جمعه، خودکار** اجرا می‌شود.

هر چند روز:

```bash
git pull
python journal.py summary
```

---

## عیب‌یابی

| خطا | درمان |
|---|---|
| `Authentication failed` | رمز عبور زدید به‌جای توکن. گام ۲ |
| `Permission denied` | توکن اسکوپ `repo` ندارد |
| `refusing to allow... workflow` | توکن اسکوپ `workflow` ندارد |
| `src refshead does not match any` | کامیت نشده — `git log` را چک کنید |
| `remote origin already exists` | `git remote set-url origin آدرس` |
| مرحلهٔ commit قرمز شد | **گام ۴ را انجام نداده‌اید** |
| اجرا اصلاً شروع نشد | زمان‌بندی تا ۱۵ دقیقه تأخیر دارد |

هر خطایی دیدید، **متن کامل خطا** را بفرستید (ولی توکن را نه).
