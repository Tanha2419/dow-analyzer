# 📤 گام ۳ — آپلود کد، بدون ترمینال

راهنمای قبلی فرض کرده بود با ترمینال و گیت راحتید. این یکی
**فقط با موس و مرورگر** است.

---

## 🎯 چه کار می‌خواهیم بکنیم؟

کد روی کامپیوتر شماست. باید برود روی گیت‌هاب. همین.

مثل آپلود عکس در اینستاگرام — فایل را می‌کشید و رها می‌کنید.

---

## گام ۳ — الف) دانلود بسته

فایل `dow-analyzer.zip` را از همین گفتگو دانلود کنید.

۲۷۰ کیلوبایت است، ۴۵ فایل. تمام کدی که ساخته‌ایم داخلش است.

### 📂 از حالت زیپ خارجش کنید

| سیستم | کار |
|---|---|
| **ویندوز** | راست‌کلیک ← `Extract All` ← `Extract` |
| **مک** | دوبار کلیک روی فایل |

حالا یک پوشه دارید به اسم `dow-analyzer` که داخلش پر از فایل است.

> 🔍 **یک چیز را چک کنید:** داخل پوشه باید فایلی به اسم
> `cron_record.py` ببینید. اگر به‌جای فایل‌ها، یک پوشهٔ دیگر
> می‌بینید، وارد آن شوید — فایل‌ها یک لایه تو هستند.

---

## گام ۳ — ب) باز کردن صفحهٔ آپلود

در مرورگر، صفحهٔ ریپویی که در گام ۱ ساختید را باز کنید.

آدرسش این شکلی است:

```
https://github.com/نام-کاربری-شما/dow-analyzer
```

چون ریپو خالی است، صفحه‌ای می‌بینید با چند جعبهٔ کد.
دنبال این جمله بگردید:

> **uploading an existing file**

روی همین عبارت آبی کلیک کنید.

### اگر این جمله را نمی‌بینید

یعنی ریپو خالی نیست. اشکالی ندارد — این آدرس را مستقیم باز کنید
(فقط `USERNAME` را عوض کنید):

```
https://github.com/USERNAME/dow-analyzer/upload/main
```

---

## گام ۳ — ج) کشیدن و رها کردن 🖱️

حالا صفحه‌ای می‌بینید که وسطش نوشته:

> **Drag files here to add them to your repository**

### کاری که می‌کنید:

۱. پوشهٔ `dow-analyzer` را که از زیپ درآوردید باز کنید
۲. **همهٔ فایل‌ها را انتخاب کنید** — `Ctrl+A` (مک: `Cmd+A`)
۳. با موس **بکشیدشان** روی آن ناحیهٔ خاکستری در مرورگر
۴. رها کنید

### ⚠️ نکتهٔ خیلی مهم

**فایل‌های داخل پوشه را بکشید، نه خود پوشه را.**

اگر خود پوشه را بکشید، همه‌چیز یک لایه تو می‌رود و ورک‌فلو
کار نمی‌کند.

```
✅ درست:  agent.py, server.py, .github/ ...
❌ غلط:   dow-analyzer/
```

### صبر کنید تا آپلود تمام شود

پایین صفحه فهرست فایل‌ها ظاهر می‌شود. چند ثانیه طول می‌کشد.

---

## گام ۳ — د) ثبت نهایی

پایین همان صفحه، جعبه‌ای هست با عنوان **Commit changes**.

۱. در کادر اول بنویسید: `نسخه اولیه` (یا هر چیزی)
۲. دکمهٔ سبز **Commit changes** را بزنید

تمام! 🎉

---

## ✅ چطور بفهمم درست شد؟

صفحهٔ ریپو را باز کنید. باید این‌ها را ببینید:

| نشانه | یعنی |
|---|---|
| فهرستی از فایل‌ها با نام `agent.py`، `server.py` | ✅ آپلود شد |
| پوشه‌ای به اسم `.github` | ✅ ورک‌فلو رفته |
| بالای صفحه: **45 files** یا نزدیک به آن | ✅ همه رفتند |

### ❗ اگر پوشهٔ `.github` را نمی‌بینید

این مهم‌ترین پوشه است — بدون آن اتوماسیون کار نمی‌کند.

گیت‌هاب گاهی پوشه‌های مخفی (که با نقطه شروع می‌شوند) را در
کشیدن‌ورها جا می‌اندازد. راه‌حل دستی:

۱. در صفحهٔ ریپو: **Add file** ← **Create new file**
۲. در کادر نام فایل، **دقیقاً** این را تایپ کنید:

```
.github/workflows/record.yml
```

> نکته: وقتی `/` تایپ می‌کنید، گیت‌هاب خودش پوشه می‌سازد.

۳. فایل `.github/workflows/record.yml` را از پوشهٔ کامپیوترتان با
   Notepad یا TextEdit باز کنید
۴. همه‌اش را کپی کنید (`Ctrl+A` بعد `Ctrl+C`)
۵. در کادر بزرگ گیت‌هاب بچسبانید (`Ctrl+V`)
۶. پایین: **Commit changes**

---

## 🎬 حالا چه؟

برگردید به راهنمای اصلی `شروع-اینجا.md` و **گام ۴** را انجام دهید:

**Settings ← Actions ← General ← Read and write permissions**

این گام را رد نکنید — بدون آن داده‌ها ذخیره نمی‌شوند.

---

## 💡 چرا این روش از ترمینال بهتر است؟

| | کشیدن‌ورها | ترمینال |
|---|---|---|
| توکن لازم دارد | ❌ نه | ✅ بله |
| دستور حفظ کردن | ❌ نه | ✅ بله |
| خطای تایپی | کم | زیاد |

**گام ۲ راهنمای اصلی (ساخت توکن) را کلاً نادیده بگیرید** — با این
روش لازم نیست.

---

## 🆘 مشکلی پیش آمد؟

| اتفاق | معنی | راه‌حل |
|---|---|---|
| «Yowza, that's a lot of files» | بیش از ۱۰۰ فایل کشیدید | فقط داخل پوشه را بکشید نه کل چیز |
| فایل‌ها آپلود شدند ولی `.github` نیست | پوشهٔ مخفی جا ماند | بخش «اگر پوشهٔ .github را نمی‌بینید» بالا |
| «Commit changes» خاکستری است | آپلود هنوز تمام نشده | چند ثانیه صبر کنید |
| همه‌چیز داخل یک پوشه رفته | خود پوشه را کشیدید | فایل‌ها را پاک کنید و دوباره |

عکس صفحه بفرستید اگر گیر کردید — کمکتان می‌کنم.
