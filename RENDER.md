# 🌐 بالا آوردن داشبورد روی اینترنت

نقشهٔ شما: **گیت‌هاب** (کد) ← **رندر** (سایت) ← **UptimeRobot** (بیدار نگه‌داشتن)

این درست است و کار می‌کند. یک نکتهٔ مهم دارد که پایین توضیح داده‌ام.

---

## ⚠️ اول این را بخوانید: تقسیم کار

رندر رایگان **دیسک دائمی ندارد**. یعنی هر بار سرویس ری‌استارت شود،
فایل‌ها پاک می‌شوند.

اگر ثبت سیگنال را روی رندر بگذاریم، **کارنامه بی‌سروصدا خالی
می‌شود** و شما فکر می‌کنید داده جمع می‌شود در حالی که نمی‌شود.

پس کار را تقسیم می‌کنیم:

| کی | چه کار |
|---|---|
| **رندر** | فقط **نمایش** داشبورد — سایتی که باز می‌کنید |
| **گیت‌هاب اکشنز** | **ثبت** سیگنال‌ها و ذخیرهٔ دائمی در مخزن |

هر دو از یک مخزن می‌خوانند. رندر داده‌ای تولید نمی‌کند، فقط نشان
می‌دهد. این‌طوری هیچ داده‌ای گم نمی‌شود.

---

## فایل‌هایی که ساخته شده

| فایل | کار |
|---|---|
| `render.yaml` | تنظیمات رندر — خودش می‌خواندش |
| `wsgi.py` | نقطهٔ ورود سرور تولیدی |
| `requirements-web.txt` | وابستگی‌های سبک‌شده |
| `/api/ping` | روت مخصوص UptimeRobot |

### چرا `requirements-web.txt` جداست؟

از فایل اصلی شش کتابخانهٔ سنگین حذف شده (`matplotlib`، `plotly`،
`bokeh`، `seaborn`، `backtesting`، `pandas-ta`). این‌ها فقط در
تحلیل محلی لازم‌اند.

نتیجه: مصرف حافظه **۲۳۰ مگابایت** — زیر سقف ۵۱۲ مگابایتی رندر.

---

# 📋 مراحل

## گام ۱ — کد روی گیت‌هاب باشد

اگر هنوز نکرده‌اید: `قدم-اول.md` را ببینید.

---

## گام ۲ — ساخت سرویس در رندر

۱. [dashboard.render.com](https://dashboard.render.com) → ثبت‌نام
   با **Sign in with GitHub** (ساده‌ترین راه)
۲. **New +** ← **Web Service**
۳. مخزن `dow-analyzer` را انتخاب کنید ← **Connect**

رندر فایل `render.yaml` را می‌بیند و همه‌چیز را خودش پر می‌کند:

| فیلد | مقدار (خودکار) |
|---|---|
| Runtime | Python 3 |
| Build Command | `pip install -r requirements-web.txt` |
| Start Command | `gunicorn wsgi:app ...` |
| Plan | **Free** |

۴. **Create Web Service**

### ⏳ صبر کنید

اولین ساخت **۵ تا ۱۰ دقیقه** طول می‌کشد. لاگ را تماشا کنید.
وقتی این را دیدید، آماده است:

```
==> Your service is live 🎉
```

آدرستان چیزی شبیه این می‌شود:

```
https://dow-analyzer.onrender.com
```

بازش کنید — داشبورد باید بیاید.

---

## گام ۳ — کلیدها (اختیاری)

در صفحهٔ سرویس: **Environment** ← **Add Environment Variable**

| نام | مقدار |
|---|---|
| `FINNHUB_KEY` | از فایل `.env` شما |
| `ALPHAVANTAGE_KEY` | همان‌جا |
| `FRED_KEY` | همان‌جا |

بدون این‌ها هم کار می‌کند — منابع اصلی کلید نمی‌خواهند.

---

## گام ۴ — UptimeRobot

رندر رایگان بعد از **۱۵ دقیقه** بی‌ترافیکی می‌خوابد و بیدار شدنش
حدود یک دقیقه طول می‌کشد. UptimeRobot هر چند دقیقه یک درخواست
می‌زند تا نخوابد.

۱. [uptimerobot.com](https://uptimerobot.com) → ثبت‌نام رایگان
۲. **+ New monitor**
۳. پر کنید:

| فیلد | مقدار |
|---|---|
| Monitor Type | **HTTP(s)** |
| Friendly Name | `داشبورد` |
| URL | `https://dow-analyzer.onrender.com/api/ping` |
| Monitoring Interval | **5 minutes** |

۴. **Create Monitor**

### ❗ حتماً `/api/ping` بگذارید، نه آدرس اصلی

این روت مخصوص همین کار ساخته شده: **۱۰ میلی‌ثانیه** جواب می‌دهد و
**هیچ تماسی با یاهو نمی‌گیرد**.

اگر مانیتور را به آدرس اصلی وصل کنید، هر ۵ دقیقه یک تحلیل کامل
اجرا می‌شود — یعنی روزی ۲۸۸ درخواست بیهوده به یاهو، که به محدودیت
نرخ می‌خورید.

---

## ✅ چک نهایی

| آزمون | انتظار |
|---|---|
| `آدرس/api/ping` | `{"ok":true,"pong":true,...}` |
| `آدرس/api/health` | اطلاعات سرویس |
| آدرس اصلی | داشبورد کامل |
| UptimeRobot | چراغ سبز |

---

## 🔢 محدودیت‌های واقعی رایگان

| مورد | سقف | ما |
|---|---|---|
| حافظه | ۵۱۲ مگابایت | ۲۳۰ ✅ |
| ساعت اجرا | ۷۵۰ در ماه | ۷۲۰ ✅ |
| دیسک دائمی | ❌ ندارد | برای همین ثبت روی گیت‌هاب است |

> ⚠️ **۷۵۰ ساعت یعنی یک سرویس تمام‌وقت.** اگر سرویس دومی بسازید،
> هر دو وسط ماه معلق می‌شوند.

---

## 🆘 مشکلات رایج

| نشانه | علت | درمان |
|---|---|---|
| `Out of memory` | بیش از ۵۱۲ مگ | مطمئن شوید `requirements-web.txt` استفاده می‌شود نه `requirements.txt` |
| ساخت شکست خورد | نسخهٔ پایتون | در `render.yaml` روی `3.12.7` تنظیم شده |
| سایت باز می‌شود ولی داده ندارد | یاهو محدود کرده | چند دقیقه صبر کنید |
| هنوز می‌خوابد | مانیتور کار نمی‌کند | آدرس را در UptimeRobot چک کنید |
| کارنامه خالی است | طبیعی است | ثبت کار گیت‌هاب اکشنز است، نه رندر |

---

## 💡 چرا هر دو (رندر + گیت‌هاب)؟

```
   گیت‌هاب اکشنز          رندر
   ─────────────          ─────
   هر ساعت ثبت      →    می‌خواند و نمایش می‌دهد
   در مخزن ذخیره         دیسکش موقتی است
   دائمی ✅              فقط نمایش
```

اگر فقط رندر داشتیم، داده‌ها پاک می‌شدند.
اگر فقط گیت‌هاب داشتیم، سایتی برای دیدن نبود.
